name: first-one
on: push
jobs:
    test-job:
        runs-on: ubuntu-latest
        steps:
            
            - name: checkout repository
              uses: actions/checkout@v7
            - name: Welcome message
              run: |
                echo "Hello World"
                cat README.md

          